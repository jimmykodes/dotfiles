setopt auto_cd           # cd into dir without needing `cd` explicetly
setopt auto_pushd        # cd automatically pushes old dir to stack
setopt pushd_ignore_dups # don't push duplicates to dir stack
setopt numeric_glob_sort # sort globs numerically (file1, file2, file10)
