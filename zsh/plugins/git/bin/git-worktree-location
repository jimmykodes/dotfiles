#!/usr/bin/env bash

main() {
	local branch="$1"
	if [[ -z $branch ]]; then
		echo "branch required"
		return 1
	fi

	git worktree list --porcelain | rg "refs/heads/$branch" -B2 | head -n 1 | awk '{print $2}'
}

main "$@"
